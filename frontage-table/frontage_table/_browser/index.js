// frontage-table has no JavaScript. Virtualisation is a memo over a scroll signal, which the
// framework already does better than a library would: the rows on screen are a fixed set of
// elements whose cells update in place. The stylesheet beside this file is the browser half.
export default {};
