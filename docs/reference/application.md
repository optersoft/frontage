# frontage.Application

The `frontage.Application` class is a core part of Frontage. It is the main entry point for creating Frontage applications. The `Application` class is used to manage the application's state, components, and pages.

::: frontage.Application
    